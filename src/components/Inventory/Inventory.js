import React from 'react';
import Header from '../Header/Header';

const Inventory = () => {
    return (
        <div>
            <Header></Header>
            <h1>Developer is sleeping broh...</h1>
        </div>
    );
};

export default Inventory;